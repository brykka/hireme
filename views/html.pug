include header.pug
div.section#quiz
    div.html
        h2 How much do you know about HTML?
    form(action='/quiz/html', method='GET')
        div.qCard
            ul
                each i in htmlData
                    include qCard.pug

        include submitButton.pug
