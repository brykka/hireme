div.aQuestion

    h3= i.id
        span :
        span= i.question

    each answer, index in i.answers

      label
        input(id=`${index}`, type="radio", name=`${i.answers}`, value=0, checked="", visibility="hidden", onclick="changeColor()")
        label(for=`${index}`)= answer

        // div#answer= answer
      script.
        function changeColor() {
            document.getElementById('answer').style.backgroundColor="black";
          }

