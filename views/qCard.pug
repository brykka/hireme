div.aQuestion

    h3= i.id
        span :
        span= i.question

    each answer, index in i.answers

      label
        input(id=`${i.id}+${index}`, type="radio", name=`${i.answers}`, visibility="hidden", onclick=`changeColor('${i.id}+${index}')`)
        label(for=`${i.id}+${index}`, id=`${i.id}+${index}`)= answer
