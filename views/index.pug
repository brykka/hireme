include header.pug
div.section#entry
    row.header
        h2 How much do you know about web development?
    row.entry
        each val in listOfQuizzes
            div.entryCard
                a(href='quiz/' + val)
                    // We indent this so that the image is a link
                    img(src='/img/' + val + '.png')
                    //- Protfile Sectoin - portfile.pug
include profile.pug
